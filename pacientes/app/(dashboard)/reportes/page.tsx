import Proximamente from "@/components/dashboard/Proximamente";

export default function ReportesPage() {
  return (
    <Proximamente
      titulo="Reportes mensuales"
      descripcion="Cirugías realizadas, pagos recibidos vs. pendientes, desglose por clínica y por rol, export a CSV (Fase 5)."
    />
  );
}
