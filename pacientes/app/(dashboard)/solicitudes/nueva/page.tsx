import Proximamente from "@/components/dashboard/Proximamente";

export default function NuevaSolicitudPage() {
  return (
    <Proximamente
      titulo="Subir foto de solicitud"
      descripcion="Extracción automática de datos (Fase 2): sube la foto de la solicitud de cirugía y el sistema propone nombre, RUT, edad, teléfono, email, clínica y fecha para tu confirmación."
    />
  );
}
