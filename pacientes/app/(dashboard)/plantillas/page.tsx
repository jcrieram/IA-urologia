import Proximamente from "@/components/dashboard/Proximamente";

export default function PlantillasPage() {
  return (
    <Proximamente
      titulo="Plantillas de preparación quirúrgica"
      descripcion="Gestión de PDFs (consentimientos, recomendaciones, credenciales) y envío por Gmail (Fase 4)."
    />
  );
}
